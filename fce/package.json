{
  "name": "fce",
  "version": "1.0.0",
  "description": "SealedFlare FCE (Flare Compute Extension) module - decrypts sealed bids and signs auction settlement results.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "settle": "tsx src/settle-auction.ts",
    "build": "tsc --noEmit",
    "test": "tsx --test src/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "libsodium-wrappers": "^0.8.4",
    "viem": "^2.55.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/libsodium-wrappers": "^0.7.14",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
